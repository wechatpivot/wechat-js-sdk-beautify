Naming Conventions
==

1. `call_conf`: when you call the api from some js on some html page
        e.g.
        var call_conf = {
            success: function () {
                // blabla
            }
        };
        wx.getLocation(call_conf);
